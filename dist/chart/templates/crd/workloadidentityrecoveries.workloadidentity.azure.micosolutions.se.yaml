{{- if .Values.crds.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
    controller-gen.kubebuilder.io/version: v0.21.0
  name: workloadidentityrecoveries.workloadidentity.azure.micosolutions.se
spec:
  group: workloadidentity.azure.micosolutions.se
  names:
    kind: WorkloadIdentityRecovery
    listKind: WorkloadIdentityRecoveryList
    plural: workloadidentityrecoveries
    singular: workloadidentityrecovery
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Complete')].status
      name: Complete
      type: string
    - jsonPath: .status.conditions[?(@.type=='Blocked')].status
      name: Blocked
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkloadIdentityRecovery is the Schema for the workloadidentityrecoveries
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WorkloadIdentityRecovery
            properties:
              previousWorkloadIdentityUid:
                description: previousWorkloadIdentityUid is the UID currently recorded
                  on the retained Azure identity.
                type: string
              workloadIdentityRef:
                description: workloadIdentityRef identifies the current WorkloadIdentity
                  instance that will receive ownership.
                properties:
                  name:
                    description: name is the name of the current WorkloadIdentity.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    description: namespace is the namespace of the current WorkloadIdentity.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  uid:
                    description: uid is the UID of the current WorkloadIdentity instance.
                    type: string
                required:
                - name
                - namespace
                - uid
                type: object
                x-kubernetes-validations:
                - message: uid must not be empty
                  rule: size(self.uid) > 0
            required:
            - previousWorkloadIdentityUid
            - workloadIdentityRef
            type: object
            x-kubernetes-validations:
            - message: spec is immutable
              rule: self == oldSelf
            - message: previousWorkloadIdentityUid must not be empty
              rule: size(self.previousWorkloadIdentityUid) > 0
            - message: previousWorkloadIdentityUid must differ from the current WorkloadIdentity
                uid
              rule: self.previousWorkloadIdentityUid != self.workloadIdentityRef.uid
          status:
            description: status defines the observed state of WorkloadIdentityRecovery
            properties:
              commitVerified:
                description: commitVerified indicates that UAMI ownership transfer
                  was read-verified.
                type: boolean
              completedTime:
                description: completedTime is when recovery completion was read-verified.
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the WorkloadIdentityRecovery
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastAttemptTime:
                description: lastAttemptTime is when reconciliation last attempted
                  recovery.
                format: date-time
                type: string
              mutationStarted:
                description: |-
                  mutationStarted indicates that recovery is forward-only and must reach a
                  completed state before its finalizer can be removed.
                type: boolean
              observedGeneration:
                description: observedGeneration is the latest generation handled by
                  the controller.
                format: int64
                type: integer
              plan:
                description: plan is the read-verified identity and trust tuple captured
                  before mutation.
                properties:
                  federatedIdentityCredential:
                    description: federatedIdentityCredential records the exact trust
                      tuple recovery applies.
                    properties:
                      audiences:
                        description: audiences are the trusted token audiences.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      issuer:
                        description: issuer is the trusted token issuer.
                        type: string
                      subject:
                        description: subject is the trusted Kubernetes ServiceAccount
                          subject.
                        type: string
                    required:
                    - audiences
                    - issuer
                    - subject
                    type: object
                  userAssignedIdentity:
                    description: userAssignedIdentity records the verified retained
                      Azure identity.
                    properties:
                      clientId:
                        description: clientId is the managed identity client ID.
                        type: string
                      id:
                        description: id is the Azure resource ID.
                        type: string
                      principalId:
                        description: principalId is the managed identity principal
                          ID.
                        type: string
                      tenantId:
                        description: tenantId is the managed identity tenant ID.
                        type: string
                    required:
                    - clientId
                    - id
                    - principalId
                    - tenantId
                    type: object
                required:
                - federatedIdentityCredential
                - userAssignedIdentity
                type: object
              startedTime:
                description: startedTime is when the first external recovery mutation
                  began.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
