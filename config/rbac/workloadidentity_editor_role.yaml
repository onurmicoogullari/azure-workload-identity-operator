# This rule is not used by the project az-workload-identity-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the workloadidentity.workloadidentity.azure.micosolutions.se.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: az-workload-identity-operator
    app.kubernetes.io/managed-by: kustomize
  name: workloadidentity-editor-role
rules:
- apiGroups:
  - workloadidentity.workloadidentity.azure.micosolutions.se
  resources:
  - workloadidentities
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - workloadidentity.workloadidentity.azure.micosolutions.se
  resources:
  - workloadidentities/status
  verbs:
  - get
