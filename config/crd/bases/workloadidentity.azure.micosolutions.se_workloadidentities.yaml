---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: workloadidentities.workloadidentity.azure.micosolutions.se
spec:
  group: workloadidentity.azure.micosolutions.se
  names:
    kind: WorkloadIdentity
    listKind: WorkloadIdentityList
    plural: workloadidentities
    singular: workloadidentity
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.clientID
      name: Client ID
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkloadIdentity is the Schema for the workloadidentities API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WorkloadIdentity
            properties:
              azure:
                description: azure configures the Azure managed identity and federated
                  credential.
                properties:
                  federatedIdentityCredentialName:
                    description: federatedIdentityCredentialName is the Azure federated
                      identity credential name.
                    maxLength: 120
                    minLength: 1
                    pattern: ^[A-Za-z0-9][A-Za-z0-9_-]*$
                    type: string
                    x-kubernetes-validations:
                    - message: field is immutable
                      rule: self == oldSelf
                  userAssignedIdentityName:
                    description: |-
                      userAssignedIdentityName is the suffix used to resolve the Azure User Assigned Managed Identity name
                      as "<namespace>-<userAssignedIdentityName>".
                    maxLength: 128
                    minLength: 1
                    pattern: ^[A-Za-z0-9][A-Za-z0-9_-]*$
                    type: string
                    x-kubernetes-validations:
                    - message: field is immutable
                      rule: self == oldSelf
                required:
                - federatedIdentityCredentialName
                - userAssignedIdentityName
                type: object
              deletionPolicy:
                default: Retain
                description: |-
                  deletionPolicy controls cleanup when this WorkloadIdentity is deleted. Delete removes Azure resources
                  according to their recorded ownership and deletes the ServiceAccount only when the operator created it.
                enum:
                - Retain
                - Delete
                type: string
              serviceAccount:
                description: serviceAccount is the Kubernetes ServiceAccount to create
                  or adopt.
                properties:
                  name:
                    description: name is the ServiceAccount name.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                    x-kubernetes-validations:
                    - message: field is immutable
                      rule: self == oldSelf
                required:
                - name
                type: object
            required:
            - azure
            - serviceAccount
            type: object
          status:
            description: status defines the observed state of WorkloadIdentity
            properties:
              azureResources:
                description: azureResources contains the Azure resources owned or
                  used by this workload identity.
                items:
                  properties:
                    id:
                      description: id is the Azure resource ID.
                      minLength: 1
                      type: string
                    kind:
                      description: kind is the Azure resource kind, for example ResourceGroup,
                        StorageAccount, or BlobContainer.
                      minLength: 1
                      type: string
                  required:
                  - id
                  - kind
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              clientID:
                description: clientID is the Azure User Assigned Managed Identity
                  client ID.
                type: string
              conditions:
                description: conditions represent the current state of the WorkloadIdentity
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              issuerURL:
                description: issuerURL is the OIDC issuer URL used by the federated
                  credential.
                type: string
              lastReconciledTime:
                description: lastReconciledTime is when the controller last handled
                  this resource.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation reconciled
                  by the controller.
                format: int64
                type: integer
              principalID:
                description: principalID is the Azure User Assigned Managed Identity
                  principal ID.
                type: string
              recovery:
                description: |-
                  recovery contains the source identity evidence needed to recover an operator-created retained identity.
                  It is present only while the WorkloadIdentity requires recovery.
                properties:
                  previousWorkloadIdentityUid:
                    description: previousWorkloadIdentityUid is the UID recorded on
                      the retained Azure identity.
                    type: string
                required:
                - previousWorkloadIdentityUid
                type: object
              serviceAccountProvenance:
                description: |-
                  serviceAccountProvenance records whether the logical ServiceAccount was created or adopted when this
                  WorkloadIdentity first established the relationship. It remains stable across ServiceAccount recreation.
                enum:
                - Created
                - Adopted
                type: string
              serviceAccountUID:
                description: serviceAccountUID is the UID of the ServiceAccount instance
                  last reconciled by the controller.
                type: string
              subject:
                description: subject is the Kubernetes service account subject trusted
                  by Azure.
                type: string
              tenantID:
                description: tenantID is the Azure tenant ID for the managed identity.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
